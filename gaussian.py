import random
import math
import numpy as np
sig = 3.0 
mu=0

dufile = open("gauss.dat","w")

for i in range(-100, 100, 1):
    p = (1/math.sqr(math.pi)*sig**2)*math.exp(-((x-mu)**2)/2*(sig**2))

    
